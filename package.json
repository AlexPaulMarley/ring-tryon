{
  "name": "ring-tryon",
  "version": "0.1.0",
  "private": true,
  "description": "WebAR ring try-on — MediaPipe Hands + Three.js, no build step.",
  "scripts": {
    "dev": "npx serve public -l 8000",
    "start": "npx serve public -l 8000"
  },
  "engines": {
    "node": ">=18"
  }
}
